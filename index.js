var alreadyInline =["Steven", "Blake", "Avi"]

function takeANumber(katzDeli, name) {
  var katzDeliLine = []
  names = ["Ada", "Grace", "Kent"]

  for ( let i =0; i<names.length; i++) {
    if(names[i] == name) {
        return `Welcome ${name[i]}, You are ${i} in line`;
    }
  }
